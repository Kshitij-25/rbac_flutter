name: rbac_ui_engine
description: >
  A production-grade Flutter/Dart package for Role-Based UI Rendering.
  Declarative, reactive, and RBAC/ABAC-compatible with Clean Architecture.
version: 1.0.0
homepage: https://github.com/Kshitij-25/rbac_flutter
repository: https://github.com/Kshitij-25/rbac_flutter
issue_tracker: https://github.com/Kshitij-25/rbac_flutter/issues

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  crypto: ^3.0.7
  dartz: ^0.10.1
  dio: ^5.9.2
  equatable: ^2.0.8
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.3.1
  json_annotation: ^4.11.0
  logger: ^2.7.0
  riverpod: ^3.2.1
  shared_preferences: ^2.5.4

dev_dependencies:
  build_runner: ^2.13.0
  fake_async: ^1.3.3
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  json_serializable: ^6.13.0
  mocktail: ^1.0.4

flutter:
